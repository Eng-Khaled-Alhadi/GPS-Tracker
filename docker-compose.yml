services:
  mysql:
    image: mysql:8.0
    container_name: gps_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-rootpassword}
      MYSQL_ROOT_HOST: "%"
      MYSQL_DATABASE: ${DB_NAME:-gps_tracker}

    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD:-rootpassword}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - gps_network

  gps-backend:
    build:
      context: ./gps_tracker_backend
      dockerfile: Dockerfile
    container_name: gps_backend
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-gps_tracker}
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD:-rootpassword}
    ports:
      - "8000:8000"       # JT808 GPS Terminal TCP Port
      - "8081:8081"       # Spring Boot REST & WebSocket Port
    networks:
      - gps_network

  gps-web:
    build:
      context: ./gps_tracker_app
      dockerfile: Dockerfile
      args:
        SERVER_HOST: ${SERVER_HOST:-176.45.55.56}
    container_name: gps_web

    restart: unless-stopped
    ports:
      - "80:80"           # HTTP (Redirect to HTTPS & ACME challenge)
      - "443:443"         # HTTPS (Web Dashboard + Secure WebSocket WSS)
    volumes:
      - ./ssl:/etc/nginx/ssl:ro
      - ./certbot/www:/var/www/certbot:ro
    networks:
      - gps_network


volumes:
  mysql_data:
    driver: local

networks:
  gps_network:
    driver: bridge

